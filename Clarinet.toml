[project]
name = "save-optimism"
authors = ["Web3 Community Builders"]
description = "A decentralized project funding platform built on Stacks blockchain"
telemetry = false
requirements = []
[contracts.project-funding]
path = "contracts/project-funding.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
